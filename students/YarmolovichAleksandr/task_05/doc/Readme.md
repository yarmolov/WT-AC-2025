# Министерство образования Республики Беларусь

<p align="center">Учреждение образования</p>
<p align="center">"Брестский Государственный технический университет"</p>
<p align="center">Кафедра ИИТ</p>
<br><br><br><br><br><br>
<p align="center"><strong>Лабораторная работа №5</strong></p>
<p align="center"><strong>По дисциплине:</strong> "Веб-технологии"</p>
<p align="center"><strong>Тема:</strong> "Серверное REST API на Node.js + Express (валидация, ошибки, Swagger)"</p>
<br><br><br><br><br><br>
<p align="right"><strong>Выполнил:</strong></p>
<p align="right">Студент 4 курса</p>
<p align="right">Группы АС-63</p>
<p align="right">Ярмоловис А.С.</p>
<p align="right"><strong>Проверил:</strong></p>
<p align="right">Несюк А.Н.</p>
<br><br><br><br><br>
<p align="center"><strong>Брест 2025</strong></p>

---

## Цель работы

* Создание REST API для ресурса на Express с валидацией входных данных
* Реализация централизованной обработки ошибок
* Генерация документации OpenAPI/Swagger UI
* Временное хранение данных в памяти приложения

---

### Вариант №24

## Ход выполнения работы

### 1. Структура проекта

- `src/app.js` — конфигурация Express приложения и middleware
- `src/server.js` — точка входа, запуск сервера
- `src/controllers/reviewsController.js` — бизнес-логика для работы с отзывами
- `src/routes/reviews.js` — маршруты API для ресурса отзывов
- `src/middleware/errorHandler.js` — централизованная обработка ошибок
- `src/middleware/validate.js` — middleware для валидации данных
- `src/schemas/review.js` — схемы валидации с использованием Zod
- `package.json` — конфигурация npm-проекта и зависимости
- `.env` — переменные окружения
- `.gitignore` — исключения для Git
- `README.md` — инструкция по запуску и документация

---

### 2. Реализованные функции

#### 2.1 Базовая структура Express

- Настроен npm-проект с скриптами `dev`/`start`
- Подключены middleware: `json()`, `cors`, `morgan`
- Сервер запускается на порту 3000

#### 2.2 REST API для ресурса "reviews"

| Метод | URL | Описание |
|-------|-----|-----------|
| GET | `/reviews` | Получить список всех отзывов |
| GET | `/reviews/:id` | Получить отзыв по ID |
| POST | `/reviews` | Создать новый отзыв |
| PATCH | `/reviews/:id` | Обновить отзыв |
| DELETE | `/reviews/:id` | Удалить отзыв |

#### 2.3 Валидация данных

Схемы валидации реализованы с использованием Zod:
- `placeName`: строка 1-100 символов
- `author`: строка 1-50 символов
- `rating`: число 1-5
- `comment`: строка 1-500 символов (опционально)
- `visitedAt`: дата в формате ISO 8601

#### 2.4 Обработка ошибок

- Централизованный error handler
- Кастомные ошибки для различных сценариев
- Корректные HTTP статус-коды (200, 201, 204, 400, 404, 422, 500)

#### 2.5 Документация Swagger

- Автоматическая генерация документации из JSDoc
- Swagger UI доступен по адресу `/docs`
- Полное описание всех эндпоинтов и схем данных

---

### 3. Запуск проекта

#### 3.1 Установка зависимостей

* Откройте проект в VSCode.
* Установите зависимости:

```bash
npm install
```

#### 3.2 Запуск сервера

* Запустить сервер в режиме разработки:

```bash
npm run dev
```

* Сервер запустится по адресу:

```bash
http://localhost:3000
```

При первом запуске API будет доступно по корневому пути.

#### 3.3 Документация Swagger

* После запуска сервера откройте в браузере:

```bash
http://localhost:3000/docs
```

* В Swagger UI доступны все эндпоинты API с возможностью интерактивного тестирования.

Все CRUD-запросы работают через локальный сервер Node.js + Express.

---

### 4. Примеры запросов

Создание отзыва (POST /reviews)

```bash
{
  "placeName": "Кофейня Уют",
  "author": "Анна",
  "rating": 5,
  "comment": "Очень вкусный кофе!",
  "visitedAt": "2024-10-15T14:30:00.000Z"
}
```

Обновление отзыва (PATCH /reviews/1)

```bash
{
  "rating": 4,
  "comment": "Обновленный комментарий"
}
```

---

### 5. HTTP Статус-коды

| Статус |         Описание          |
|--------|---------------------------|
| 200    | Успешный запрос           |
| 201    | Ресурс создан             |
| 204    | Ресурс удален             |
| 400    | Неверный запрос           |
| 404    | Ресурс не найден          |
| 422    | Ошибка валидации          |
| 500    | Внутренняя ошибка сервера |

---

## Таблица критериев

| Критерий                                                                | Баллы |  Выполнено |
|-------------------------------------------------------------------------|-------|------------|
| Структура / семантика API                                               |  20   |  ✅ / ✅  |
| Функциональность CRUD                                                   |  25   |  ✅ / ✅  |
| Качество интерфейса API (валидация, ошибки, статус‑коды)                |  20   |  ✅ / ✅  |
| Качество кода / архитектуры                                             |  15   |  ✅ / ✅  |
| Тесты/проверки (по желанию supertest)                                   |  10   |  ✅ / ✅  |
| Документация/инструкция                                                 |  10   |  ✅ / ✅  |

### Дополнительные бонусы

| Бонус                                                                | Баллы | Выполнено |
| -------------------------------------------------------------------- | ----- | --------- |
| Фильтрация/поиск/сортировка.                                         | 10    | ❌ / ✅  |
| Пагинация с метаданными (total, limit, offset).                      | 10    | ❌ / ✅  |
| Версионирование API (/api/v1).                                       | 10    | ❌ / ✅  |

---

## Ссылка на публикацию

👉 Вставьте ссылку на GitHub Pages: https://yarmolov.github.io/WT-LAB5/

## Вывод

В ходе лабораторной работы было успешно разработано REST API для управления отзывами о местах с использованием Node.js и Express. Реализованы все обязательные требования: CRUD операции, валидация входных данных с Zod, централизованная обработка ошибок и автоматическая генерация документации через Swagger. API возвращает корректные HTTP статус-коды и предоставляет понятные сообщения об ошибках. Бонусные функции (пагинация, поиск, версионирование) были сознательно удалены для соответствия минимальным требованиям лабораторной работы. Приложение готово к использованию и демонстрации.
